<% if resource.errors.any? %>
  <div id="error_explanation" data-turbo-cache="false" class="mb-2 px-4 py-2 rounded-md text-white bg-rose-600">
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <% if message.include?("ユーザーネーム") %>
          <li>・ <%= message %></li>
        <% end %>
      <% end %>
      <% resource.errors.full_messages.each do |message| %>
        <% unless message.include?("ユーザーネーム") %>
          <li>・ <%= message %></li>
        <% end %>
      <% end %>
    </ul>
  </div>
<% end %>
